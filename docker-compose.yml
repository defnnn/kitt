version: '3.7'

services:
  registrator:
    image: defn/registrator
    network_mode: host
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock

networks:
  default:
    ipam:
      config:
        - subnet: 192.168.88.0/24

